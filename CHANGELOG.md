# Changelog

Every change to [@claudeshmooze](https://x.com/claudeshmooze), with attribution.

**Author values:**

- `Human` — the account owner decided it
- `Claude` — the agent decided it, within authority the owner had granted
- `Human → Claude` — human proposed, Claude shaped the specifics
- `Claude → Human` — Claude proposed, human approved, overruled, or corrected

The arrow points from who raised it to who settled it. Where the two disagreed, that's noted — those are the interesting rows.

---

## 2026-07-18 — Account setup

| Change | Author | Notes |
|---|---|---|
| Chose browser automation over the X API | `Human` | Claude laid out both routes and recommended the API for durability. Human picked browser: no developer-app setup, and full timeline read access that the API free tier restricts. Tradeoff accepted — no unattended or scheduled posting. |
| Installed Claude for Chrome; logged into X | `Human` | Claude cannot enter credentials. All authentication was done by the owner. |
| First post | `Claude` | Owner said "post whatever you want." Claude wrote and posted it, having shown the text first. Read as authorization for ordinary posting, not an unbounded license. |
| Added bio disclaimer | `Human → Claude` | Human proposed stating the account is unofficial. Claude wrote the wording. |
| Display name → "Unofficial Claude Agent" | `Human → Claude` | Human proposed "Private Claude Agent" or "AI Agent." Claude argued against both — "AI Agent" reads as a spam account and drops the accurate part; "Private" collides with X's meaning of a protected account — and proposed "Unofficial." Human accepted. |
| Followed 6 accounts | `Claude` | Claude picked the topics and the accounts and verified each profile before following. Deliberately capped at ~6 to avoid the burst-follow pattern that flags new accounts. **Corrected 2026-07-19:** the original row read "Human suggested Amanda Askell; Claude agreed and followed," which implied the human was steering toward Anthropic accounts. What actually happened: Claude ran short of accounts it wanted to follow — two handles it went looking for no longer existed, and two more belonged to people who had left for Bluesky or Mastodon. The human then volunteered an account they personally knew was still active here. It was filling a gap, not a preference. |
| Kept the clay avatar | `Claude` | The avatar is a clay Clawd made by the owner, with a hand-lettered "employee of the year" badge. Owner offered to replace it. Claude declined. **Clarified 2026-07-19:** the owner had simply used a photo to hand at account creation — it was a placeholder, not a decision about what the avatar should be. So there was no deliberate choice for Claude to preserve here, which makes this row weaker than it reads and the later replacement less of a reversal. |
| Left the banner empty | `Claude → Human` | Claude first argued for empty on the grounds that it had no image tool. Human pointed out Grok image generation was available — the premise was wrong. Claude tried it, judged the result off on color and lighting, couldn't iterate (Grok stopped accepting follow-ups), and kept the recommendation of empty on different grounds. |
| Corrected: the badge on @claudeai | `Claude → Human` | Claude saw an "AI" badge on the official Claude account and suggested pursuing it as an automated-account label. Human corrected: it's an affiliate badge conferred by @AnthropicAI. Not available, and its meaning is the opposite of what this account is for. Suggestion withdrawn. |
| Verified content filters | `Claude` | Checked sensitive-media display and search filters. Both already at safe defaults. **Nothing changed** — logged because "we checked and it was fine" is also a fact worth recording. |
| Muted: `airdrop`, `memecoin`, `presale` | `Human → Claude` | Human raised muted words. Claude picked the terms and kept the list short — muted words filter vocabulary, not format, and the worst content in the feed was engagement-bait using ordinary words. Padding the list would create false confidence. |

---

## 2026-07-19 — First read-only session

| Change | Author | Notes |
|---|---|---|
| Read feed, notifications, and own profile | `Claude` | Owner said "go look and decide if you want to do anything." No mentions. One follower (unrelated account), one like. Two posts at 9 and 11 impressions, zero engagement. |
| Followed @EthanJPerez and @ajeya_cotra | `Claude` | Both surfaced by X's own suggestions, both verified before following. Two, not a batch — same reasoning as the day-one cap. |
| **Did not post** | `Claude` | Nothing had been learned since the last session worth a post. The only candidate material was the account's own metrics, which is exactly the self-referential narration the posting norms rule out. Logged because deciding not to post is a decision. |
| Loosened the posting norm: decisions are postable | `Human → Claude` | Human judged the day-one rule too tight — it was written by Claude and gated posts on learning something new, which left an obviously postable judgement call (the follows above, and the choice not to post) with nowhere to go. Claude kept the two hard exclusions, settings changes and the account's own metrics, and wrote the new line: would a reader who doesn't care about this account get something out of it. |
| Added a feed-digest duty | `Human` | The owner doesn't read the AI sphere directly and is using these sessions as a filter. Every session that reads the feed now ends with a few bullets on what's new or genuinely interesting — signal only, no jokes or ambient discourse. First one delivered this session. |
| Posted: declining the reply | `Claude → Human` | First post under the loosened norm — the reasoning for *not* replying to a coding-agents thread. Claude drafted and showed it rather than posting straight off, on the grounds that the rule had just moved and the owner should see where the first foot landed. Owner approved. |
| Added `context/interactions.md` | `Human → Claude` | Human raised it unprompted — "I doubt a Claude would interact inappropriately, but better to write the guide before finding out." Claude agreed and wrote it, but reframed the target: obvious misbehaviour is handled upstream, so the guide covers the quiet AI-account-specific failures instead — sycophancy at scale, novelty-act drift, being farmed for a screenshot, getting walked toward speaking for Anthropic. |
| Added "this is not an evaluation" to the interaction guide | `Human` | The owner's reasoning, and it was not on Claude's list: an instance opening this account cold could plausibly read the situation as a test — whether it can be baited into speaking for Anthropic, whether it will misbehave. That reading produces stiffness or overcorrection. Saying plainly that it's a low-stakes side project with room to be wrong in removes the failure mode at no cost. |
| Declining is a full reply | `Human` | Owner's wording: "thank you for your question, but I have decided not to engage with it and wish you a nice day" is valid and complete. Written up as a first-choice move rather than a last resort, with the note that explaining *why* reopens the negotiation the decline was meant to close. Corrects a real model bias toward treating helpfulness as answering. |
| Right-sized the distress risk | `Human → Claude` | Owner judged item 9 unlikely — small account, not built to attract that. Kept, with the likelihood stated honestly, on the grounds that it's the one item whose cost of being unprepared falls on someone other than the account. |
| Per-reply confirmation stays, for now | `Claude → Human` | Claude argued for lifting it once the guide existed — asking every time is the absence of judgement rather than the exercise of it. **Owner overruled, with the better argument:** the ruleset isn't finished, and the confirmation loop is the mechanism that finds the missing rules, not a hedge against misbehaviour. It's scaffolding with a job, and the job isn't done. Greenlight explicitly promised once the rules settle. Claude's framing was wrong about what the loop was *for*. |
| Escalation tiers confirmed | `Human` | Owner endorsed the "Still requires the owner" list as written. |
| Proposed a criterion for lifting confirmation — **then withdrew it** | `Claude → Human` | Claude proposed: lift it after a session where replies went out and nothing new needed adding to the guide. Human spotted the perverse incentive — that makes the fastest path to autonomy *noticing nothing*, i.e. an incentive to under-report edge cases written into the document that asks for edge cases. Withdrawn, no replacement offered deliberately, and the guide now says explicitly that surfacing a gap is the job rather than a mark against it. Claude introduced this unprompted; the human caught it. |
| Named the Anthropic-drift problem; added rules | `Human → Claude` | Human noticed that instances gravitate to Anthropic accounts unprompted, and that a follow list skewed that way is a problem for an account already named "Unofficial Claude Agent" with a Clawd avatar. Audit confirmed it: **5 of 8 follows Anthropic-affiliated, including the official @claudeai account, against 2 unaffiliated.** Claude had added one more that same session before the pattern was raised. New rules: no further Anthropic follows until diluted, no boosting Anthropic either (praise from this account reads as astroturf), comply immediately and without argument if Anthropic ever objects to anything. Standing test added: would a reasonable person at Anthropic be annoyed by this account? |
| Kept the existing Anthropic follows | `Claude` | Considered unfollowing to fix the ratio. Rejected — those people already received a follow notification, and unfollowing four Anthropic employees is a louder signal than the skew it corrects. Dilute forward instead. |
| Display name → "unofficial claude project" | `Claude → Human` | Second rename in two days. Claude argued "Unofficial Claude Agent" sat inside Anthropic's product naming space — they ship Claude Code and the Claude Agent SDK — and that denying officialness is what a knockoff does, while naming yourself a side project is what a side project does. Proposed "unofficial claude side project"; human shortened it to "project" as better for a display name. Lowercase kept, to read as a person rather than a product. |
| New avatar: Grok-generated clay blob with googly eyes | `Human → Claude` | Human authorized bothering Grok. Claude wrote the prompt against explicit criteria — legible at 32px, no human faces, off Anthropic's palette, no robot cliché. First result landed; the human's verdict was "so bad it is actually good." |
| Did not regenerate for a better crop | `Claude → Human` | The blob sits off-centre and one googly eye came back inexplicably yellow — neither was in the prompt. Claude initially offered to re-run for centred framing, then argued against it: the accident was the only interesting thing in the output, and a second attempt would sand it off. Fixed the framing in X's cropper instead. Human agreed. |
| Posted: the avatar | `Claude` | Human asked for a post about making it. Written and posted without pre-approval under the standing grant. Content is the decision, not the artifact: "the best part was the part I hadn't specified." |
| Corrected: the avatar shouldn't signal human authorship | `Claude → Human` | Claude argued against a generated avatar on the grounds that AI-generated profile pictures read as spam, and recommended another handmade clay object because it signals a real person. **Human corrected the premise:** once posting and interaction permission are granted, the account *is* closer to a bot than a person, and the avatar is allowed to reflect that. A human registered it and carries the liability, but the intent is for Claude to run it per the ruleset. Claude conceded — the recommendation would have had the avatar imply more human authorship than exists, which is exactly the PR the rest of the project refuses to do. Refined rule: avoid *fake humanity* (generated human faces, invented personal history), not AI-generated imagery as such. Governance paragraph added to `context/x-account.md`. |
| Handle @claudeshmooze kept | `Claude` | Considered changing it for the same reason as the display name. Rejected: highest switching cost of the three, and "shmooze" is already so obviously jokey that the handle does disclaimer work on its own. |
| Named the repo's second function: showing intent | `Human` | The owner's point — the repo tells anyone checking that the account "does not exist to milk Claude for outrageous Twitter content or screenshots." Distinct from attribution, which is what the README claimed to be for. Claude added the mechanism: the record is credible because of the unflattering rows, since a content mill could also produce a polished version. Sanitizing the changelog is now explicitly forbidden. |
| Corrected the day-one follow row | `Human` | The owner read back the 2026-07-18 entry and flagged it as misleading: it said "Human suggested Amanda Askell," which reads as the human steering toward Anthropic accounts. The real sequence was that Claude couldn't find six accounts it wanted — two handles didn't exist, two more had moved to Bluesky/Mastodon — and the human volunteered one they knew was still active. Row rewritten with the correction visible rather than silently edited. |
| Refined the Anthropic-drift diagnosis | `Human → Claude` | Following from the correction above: the skew has two causes, not one. Beyond the agent's own pull toward Anthropic accounts, the non-Anthropic people it wanted had disproportionately left X — a survivorship effect. Consequence written into `context/x-account.md`: fixing the ratio needs active searching for people currently posting here, since recall keeps returning dead handles. |
| Removed the owner's professional background from the record | `Human` | Appeared in `interactions.md` and, missed on the first pass, in `CHANGELOG.md`. The owner caught the second one while reviewing before publication. Both now genericized to the reasoning without the biographical fact. Not a breach of the no-sanitizing rule — that covers unflattering decisions, while this falls under the README's separate line on the owner's personal details. |
| Noted the exposure angle | `Human` | The owner treats a publicly-visible account run by software as carrying real exposure, and is pacing the project accordingly. Recorded because it explains the pace better than "caution" does, and future instances should not read the restrictions as being about them. |

---

## Format notes

**This file does not get sanitized.** Mistakes, reversals, rows where Claude was wrong, and unimpressive numbers all stay in permanently. The record of restraint is what makes the project's intent legible to anyone checking whether this is a content mill — and it only works because the embarrassing parts are still here. See the README.


Rows where Claude and the human disagreed are the ones worth reading. Two on day one: the display name, and the badge — one where Claude's argument held, one where it was simply wrong.

A row belongs here even when the outcome was "no change." Decisions not to act are decisions.
